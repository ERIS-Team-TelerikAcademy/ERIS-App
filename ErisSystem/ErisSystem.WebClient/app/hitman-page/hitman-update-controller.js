'use strict';
app.controller('hitmanUpdateController', ['$scope', '$routeParams', 'authData', 'hitmanData',
    function ($scope, authData, hitmanUpdateData) {
        $('body').on('click', '#save', function(e){

        });
    }]);